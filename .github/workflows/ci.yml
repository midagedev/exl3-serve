name: check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      # No GPU and no exllamav3 on the runner: the suite drives the fake engine,
      # which is the point of the engine seam.
      - run: pip install -e '.[test]'
      - run: pytest -q
